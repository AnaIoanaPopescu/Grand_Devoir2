package com.example.grand_devoir2;

public interface ComparableParCharacteristique {

    // Metodă pentru compararea caracteristicii de atac
    int comparerAttaque(Object o);

    // Metodă pentru compararea caracteristicii de apărare
    int comparerDefense(Object o);

    // Metodă pentru compararea caracteristicii de sănătate
    int comparerSante(Object o);
}

